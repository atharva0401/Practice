#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
    freopen("cowsignal.in" , "r" , stdin);
    freopen("cowsignal.out" , "w" , stdout);
    int m , n , k;
    string s;
    cin >> m >> n >> k;
    while(getline(cin ,s ))
    {
        for(int j = 0; j < k; j++)
        {
            char a[n*k];
            for(int i : s)
            {
                for(int l = 0 ; l < k; l++)
                {
                    
                }
            }
        }

    }
}